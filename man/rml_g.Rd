% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rml_g.R
\name{rml_g}
\alias{rml_g}
\title{Fit a global ML model across all bottom-series}
\usage{
rml_g(
  approach,
  hat,
  obs,
  params = NULL,
  seed = NULL,
  early_stopping_rounds = 0L,
  validation_split = 0,
  ...
)
}
\arguments{
\item{approach}{character; one of \code{"lightgbm"}, \code{"xgboost"}, \code{"ranger"},
\code{"mlr3"}, \code{"catboost"}.}

\item{hat}{numeric matrix of features, dimension \verb{T_obs x ncol_hat}.
Shared across series.}

\item{obs}{numeric matrix of observations, dimension \verb{T_obs x p}, where
\code{p} is the number of bottom-series.}

\item{params}{named list of backend-specific hyperparameters.}

\item{seed}{integer seed for reproducibility.}

\item{early_stopping_rounds}{integer; \code{0} disables early stopping.}

\item{validation_split}{fraction of stacked rows reserved for validation
(\code{0} disables).}

\item{...}{passed to backend.}
}
\value{
list of class \code{rml_g_fit} with elements \code{fit}, \code{approach},
\code{series_id_levels}, \code{feature_importance}, \code{ncol_hat}.
}
\description{
Stacks all series into a single training set with \code{series_id} as a
categorical feature, fits one model across all series, and returns an
\code{rml_g_fit} object. Per-series predictions are produced by the
(forthcoming) \code{predict.rml_g_fit} method (T7.5).
}
