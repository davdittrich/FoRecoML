% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rml_g.R
\name{predict.rml_g_fit}
\alias{predict.rml_g_fit}
\title{Predict from a global ML fit object}
\usage{
\method{predict}{rml_g_fit}(object, newdata, series_id = NULL, ...)
}
\arguments{
\item{object}{An \code{rml_g_fit} object returned by \link{rml_g}, \link{csrml_g},
\link{terml_g}, or \link{ctrml_g}.}

\item{newdata}{Numeric matrix (\verb{n_rows x ncol_hat}) of features.  Must
have the same column layout as the \code{hat} matrix used during training
(i.e. \code{ncol_hat} columns, no \code{series_id} column).}

\item{series_id}{Character or factor vector of length \code{n_rows} giving the
series identifier for each row of \code{newdata}.  All values must appear in
\code{object$series_id_levels}.  If \code{NULL}, all training-level series are
cycled in order (one row of \code{newdata} replicated for every series level).}

\item{...}{Ignored.}
}
\value{
Numeric vector of predictions, one per row of the expanded
\code{newdata} (after \code{series_id} replication when \code{series_id = NULL}).
}
\description{
Predict from a global ML fit object
}
