% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rml_g.R
\name{csrml_g}
\alias{csrml_g}
\title{Cross-sectional reconciliation with a global ML model}
\usage{
csrml_g(
  base,
  hat,
  obs,
  agg_mat,
  approach = "lightgbm",
  normalize = c("none", "zscore", "robust"),
  scale_fn = "gmd",
  params = NULL,
  seed = NULL,
  early_stopping_rounds = 0L,
  validation_split = 0,
  ...
)
}
\arguments{
\item{base}{base forecasts matrix (\verb{n x h}); rows = all series (upper +
bottom), columns = forecast horizons. Required for API consistency with
the series-level wrappers; not used for fitting.}

\item{hat}{numeric feature matrix (\verb{T_obs x ncol_hat}), shared across series.}

\item{obs}{numeric observation matrix (\verb{T_obs x p}), one column per
bottom-level series.}

\item{agg_mat}{cross-sectional aggregation matrix (\verb{n_agg x p}).}

\item{approach}{character; ML backend. One of \code{"lightgbm"}, \code{"xgboost"},
\code{"ranger"}, \code{"mlr3"}, \code{"catboost"}.}

\item{normalize}{pre-normalization applied to \code{hat} before fitting:
\code{"none"} (default), \code{"zscore"}, or \code{"robust"}.}

\item{scale_fn}{scale estimator for \code{normalize = "robust"}. See
\link{normalize_stack}.}

\item{params}{named list of backend hyperparameters.}

\item{seed}{integer for reproducibility.}

\item{early_stopping_rounds}{integer; \code{0} disables early stopping.}

\item{validation_split}{fraction of stacked rows reserved for validation
(\code{0} disables).}

\item{...}{passed to \link{rml_g}.}
}
\value{
\code{rml_g_fit} object with additional fields \code{agg_mat}, \code{norm_params},
and \code{framework = "cs"}.
}
\description{
Normalizes \code{hat} (optionally), fits a single global ML model across all
bottom-series via \link{rml_g}, stores reconciliation metadata on the returned
\code{rml_g_fit} object, and returns it. Reconciled forecasts are produced by
calling \link{predict.rml_g_fit} (T7.5) on the returned object.
}
